// jeg har valgt at udfører denne opgave ved gemmen søge mit Arrayt for det største negative nummer
/*
int[] numbers = {1, 2, 3, -4, 5};

int Negative = 0;

for (int i = 0; i < numbers.Length ; i++)
{
    if ( Negative > numbers[i])
    {
        Negative = numbers[i];
 
    }
}
Console.Write( Negative );
*/